<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.beingfoodie">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app"
        android:label="Foodie"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BeingFoodie">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FoodMenu" />
        <activity android:name=".SignUp" />
        <activity android:name=".staff" />
        <activity android:name=".LogIn" />
        <activity android:name=".Logup" />
        <activity android:name=".DetailsStaff" />
        <activity android:name=".order_placed"></activity>
    </application>

</manifest>